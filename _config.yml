plugins:
  - jekyll-sitemap

kramdown:
  auto_ids: true

defaults:
  - scope:
      path: ""
      type: "posts"
    values:
      permalink: /:collection/:name
  - scope:
      path: "assets/img"
    values:
      image: true
  - scope:
      path: ""
    values:
      layout: "default"